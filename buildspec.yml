version: 0.2

phases:
  pre_build:
    commands:
      - sudo rm -rf node_modules
  build:
    commands:
      - cp appspec.yml app/appspec.yml
      - cd app
      - npm install
  post_build:
    commands:
      - npm run build
artifacts:
  files:
    - '**/*'
  base-directory: app
